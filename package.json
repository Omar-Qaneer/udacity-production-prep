{
  "name": "udacity-production-prep",
  "version": "1.0.0",
  "description": "A flashcard web app optimized for production with testing (Mocha, Cypress), automation (Gulp), and bundling (Parcel) as part of the Udacity front-end project.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "gulp": "gulp",
    "start": "gulp"
  },
  "author": "Omar Qaneer",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@eslint/js": "^9.26.0",
    "eslint": "^9.26.0",
    "globals": "^16.1.0"
  },
  "dependencies": {
    "gulp": "^5.0.0",
    "gulp-cli": "^3.0.0",
    "gulp-shell": "^0.8.0",
    "parcel": "^2.15.1"
  }
}
